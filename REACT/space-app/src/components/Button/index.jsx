import styled from "styled-components"

const Button = styled.button`
  background: none;
  border: none;
  cursor: pointer;

  & > img {
    width: 22px;
    height: 22px;
  }
`


export default Button
